<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:theme="@style/Theme.SpendSass">

        <!-- Onboarding is the launcher activity -->
        <activity android:name=".screens.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".screens.login.LoginActivity"/>
        <activity android:name=".screens.register.RegisterActivity"/>
        <activity android:name=".screens.dashboard.DashboardActivity"/>
        <activity android:name=".screens.profile.ProfileActivity"/>
        <activity android:name=".screens.settings.SettingsActivity"/>
    </application>

</manifest>